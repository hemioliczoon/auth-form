export type Plan = {
  // this stores url to the icon
  icon?: string,
  name: string,
  id: number,
  priceMonth: number,
  // % discont if user would rather pay yearly 
  discountYear: number
  disc: string
}
